GAME INTEGRATION - NEXT STEPS
==============================

CURRENT STATE:
--------------
✅ Matchmaking endpoint working (/api/game/matchmaking)
✅ Game backend runs on port 4000 (WebSocket server)
✅ Game frontend on port 5174
✅ Match status: MATCHMAKING → STARTING → IN_PROGRESS → FINISHED
✅ Database stores matches with playerOneId, playerTwoId, status

WHAT'S MISSING:
---------------
1. Notify players when match is ready (status = "STARTING")
2. Redirect both players to game frontend (port 5174)
3. Game backend needs to fetch match data from main backend DB
4. Game backend reports results back to main backend when game ends

IMPLEMENTATION OPTIONS:
-----------------------

OPTION A - POLLING (Simple, quick to implement):
  - Frontend polls /api/game/matchmaking/status endpoint every 2 seconds
  - When status="STARTING", redirect to game frontend with matchId
  - Game frontend connects to game backend WebSocket with matchId

OPTION B - WEBSOCKET (Better UX, more setup):
  - Install @fastify/websocket in main backend
  - Real-time notifications when match is ready
  - Push-based instead of polling

NEXT TASKS:
-----------
1. Add status check endpoint: GET /api/game/matchmaking/:matchId
2. Frontend: Poll for match status after calling matchmaking
3. Game backend: Accept matchId parameter and fetch player names from main backend
4. Game backend: POST results to main backend when game ends
5. Update match status to "FINISHED" in database

ARCHITECTURE:
-------------
Main Backend (port 3000) - Fastify, handles auth/matchmaking/DB
Game Backend (port 4000) - Express + WebSocket, runs game logic
Main Frontend (port 5173) - User login, matchmaking UI
Game Frontend (port 5174) - Renders the actual Pong game

FILES TO MODIFY:
----------------
- backend/src/routes/Matchmaking.ts (add status endpoint)
- frontend/src/components/* (add polling/redirect logic)
- game/backend/src/app.ts (accept matchId, fetch match data)
- game/backend/src/gameManager.ts (handle match completion)
