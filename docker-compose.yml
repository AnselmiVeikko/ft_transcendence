version: "3.9"
services:
  game-module:
    image: node:20-alpine
    container_name: pong-game
    working_dir: /app
    ports:
      - "5173:5173"
    volumes:
      - ./game:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev -- --host"